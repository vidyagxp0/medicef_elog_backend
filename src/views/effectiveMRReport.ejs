<!DOCTYPE html>
<html>

<head>

    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 14px;
            margin: 20px;
            line-height: 1.6;
        }

        .report-content {
            margin-top: 30px;
            /* Increase margin top for more spacing */
        }

        h1,
        h2 {
            margin-bottom: 20px;
            text-align: left;
            /* Align headings to the left */
            clear: both;
            /* Clear floats to prevent overlap */
        }

        h1 {
            font-size: 24px;
        }

        h2 {
            font-size: 18px;
            color: #0066CC;
        }

        p {
            font-size: 14px;
            margin-bottom: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            /* Increase margin bottom for more spacing */
            border: 1px solid #000;
            /* Add border to the table */
        }

        th,
        td {
            border: 1px solid #000;
            /* Add border to table cells */
            padding: 10px;
            /* Adjusted padding for better spacing */
            text-align: left;
        }

        th {
            background-color: #f8f8f8;
            font-weight: bold;
        }

        .remarks {
            page-break-inside: avoid;
        }

        .heading {
            padding-top: 35px;
        }
    </style>


<body>
    <div class="report-content">
        <h2 class="heading">Media Records</h2>
        <% if (reportData?.MediaRecords && reportData?.MediaRecords.length> 0) { %>
            <% reportData?.MediaRecords.forEach(record=> { %>
                <div class="listItem">
                    <table>
                        <tr>
                            <th>Unique Id</th>
                            <td>
                                <%= record?.unique_id || "Not Applicable" %>
                            </td>
                            <th>Date</th>
                            <td>
                                <%= record?.date || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>
                            <th>Name Medium</th>
                            <td>
                                <%= record?.name_medium || "Not Applicable" %>
                            </td>
                            <th>Name Of Prepration</th>
                            <td>
                                <%= record?.date_of_preparation || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>
                            <th>Date Of Use</th>
                            <td>
                                <%= record?.date_of_use || "Not Applicable" %>
                            </td>
                            <th>Lot No.</th>
                            <td>
                                <%= record?.lot_no || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>
                            <th>Number Of Prepared Plate</th>
                            <td>
                                <%= record?.no_of_plate_prepared || "Not Applicable" %>
                            </td>
                            <th>Number Of Plate Used</th>
                            <td>
                                <%= record?.no_of_plate_used || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>
                            <th>Used For</th>
                            <td>
                                <%= record?.used_for || "Not Applicable" %>
                            </td>
                            <th>Balance Number Plate</th>
                            <td>
                                <%= record?.balance_no_plate || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>
                            <th>Checked By</th>
                            <td>
                                <%= record?.reviewed_by || "Not Applicable" %>
                            </td>
                             <th>Signature</th>
                             <td>
                                <%= record?.signature || "Not Applicable" %>
                            </td>
                        </tr>
                    </table>
                </div>
                <hr />
                <% }) %>
                    <% } else { %>
                        <p>No Records Found.</p>
                        <hr />
                        <% } %>

                                        <div class="remarks">
            <h2 class="heading">Additional</h2>
            <table>
                <tr>
                    <th>Additional Attachment</th>
                    <td>
                        <% if (reportData?.additionalAttachment) { %>
                            <a href="<%= reportData.additionalAttachment %>" target="_blank">View File</a>
                        <% } else { %>
                            Not Applicable
                        <% } %>
                    </td>
                </tr>
                <tr>
                    <th>Addtional Info</th>
                    <td><%= (reportData?.additionalInfo || "Not Applicable") %></td>
                </tr>
                
            </table>
        </div>
                            <hr />
    </div>
</body>

</html>