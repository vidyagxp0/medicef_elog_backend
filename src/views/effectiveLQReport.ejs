<!DOCTYPE html>
<html>

<head>

    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 14px;
            margin: 20px;
            line-height: 1.6;
        }

        .report-content {
            margin-top: 30px;
            /* Increase margin top for more spacing */
        }

        h1,
        h2 {
            margin-bottom: 20px;
            text-align: left;
            /* Align headings to the left */
            clear: both;
            /* Clear floats to prevent overlap */
        }

        h1 {
            font-size: 24px;
        }

        h2 {
            font-size: 18px;
            color: #0066CC;
        }

        p {
            font-size: 14px;
            margin-bottom: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            /* Increase margin bottom for more spacing */
            border: 1px solid #000;
            /* Add border to the table */
        }

        th,
        td {
            border: 1px solid #000;
            /* Add border to table cells */
            padding: 10px;
            /* Adjusted padding for better spacing */
            text-align: left;
        }

        th {
            background-color: #f8f8f8;
            font-weight: bold;
        }

        .remarks {
            page-break-inside: avoid;
        }

        .heading {
            padding-top: 35px;
        }
    </style>


<body>
    <div class="report-content">
        <h2 class="heading">Loaded Quantity Records</h2>
        <% if (reportData?.LoadedQuantityRecords && reportData?.LoadedQuantityRecords.length> 0) { %>
            <% reportData?.LoadedQuantityRecords.forEach(record=> { %>
                <div class="listItem">
                    <table>
                        <tr>
                            <th>Unique Id</th>
                            <td>
                                <%= record?.unique_id || "Not Applicable" %>
                            </td>
                            <th>date</th>
                            <td>
                                <%= record?.date || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>
                            <th>Product Name</th>
                            <td>
                                <%= record?.product_name || "Not Applicable" %>
                            </td>

                            <th>Batch Number</th>
                            <td>
                                <%= record?.batch_no || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>

                            <th>Batch Size</th>
                            <td>
                                <%= record?.batch_size || "Not Applicable" %>
                            </td>
                            <th>Container Size</th>
                            <td>
                                <%= record?.container_size || "Not Applicable" %>
                            </td>

                        </tr>
                        <tr>
                            <th>Theoretical Production</th>
                            <td>
                                <%= record?.theoretical_production || "Not Applicable" %>
                            </td>
                            <th>Loaded Quantity</th>
                            <td>
                                <%= record?.loaded_quantity || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>
                            <th>Yield</th>
                            <td>
                                <%= record?.yield || "Not Applicable" %>
                            </td>
                            <th>Remarks</th>
                            <td>
                                <%= record?.remarks || "Not Applicable" %>
                            </td>
                        </tr>
                        <tr>
                            <th>Checked By</th>
                            <td>
                                <%= record?.reviewed_by || "Not Applicable" %>
                            </td>
                        </tr>
                    </table>
                </div>
                <hr />
                <% }) %>
                    <% } else { %>
                        <p>No Records Found.</p>
                        <hr />
                        <% } %>

                           <div class="remarks">
            <h2 class="heading">Additional</h2>
            <table>
                <tr>
                    <th>Additional Attachment</th>
                    <td>
                        <% if (reportData?.additionalAttachment) { %>
                            <a href="<%= reportData.additionalAttachment %>" target="_blank">View File</a>
                        <% } else { %>
                            Not Applicable
                        <% } %>
                    </td>
                </tr>
                <tr>
                    <th>Addtional Info</th>
                    <td><%= (reportData?.additionalInfo || "Not Applicable") %></td>
                </tr>
                
            </table>
        </div>
                            <hr />
    </div>
</body>

</html>